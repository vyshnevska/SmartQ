<% if @valid %>
  $('#categories').prepend("<%= escape_javascript(render :partial => 'admin/categories/category', :locals => { category: @category, index: -1 }) %>");
<% else %>
  $('#alert-notifications').html("<%= escape_javascript(render :partial => 'layouts/notifications') %>");
<% end %>
