%p
  %b= "#{@user_assessment.user_name.capitalize}, You got #{@user_assessment.grade} points"

= render 'user_assessment', :locals => { :user_assessment => @user_assessment, :view_mode => @view_mode }


= link_to 'Back', user_assessments_path
